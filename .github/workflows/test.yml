name: tests
on: [push, pull_request]
jobs:
  audit-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - run: node --experimental-strip-types --no-warnings test/audit-check.mjs
